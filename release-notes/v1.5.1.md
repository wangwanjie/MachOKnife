## Highlights

- 补齐 Finder `Open With` 的 Mach-O 文档类型声明，让 `dylib`、`Mach-O` 可执行文件、对象文件和静态库更稳定地出现 `MachOKnife`
- 针对无后缀可执行文件补充 `public.unix-executable` 关联，修复归档产物内裸 Mach-O 在 Finder 里看不到 `MachOKnife` 的问题
- 为 Finder 打开链路与项目配置新增回归测试，覆盖系统打开文件时复用现有工作区加载路径的行为
- 归档并同步 `register-macho-document-types` OpenSpec 规范，确保主规范与实现保持一致
